apiVersion: v1
kind: Service
metadata:
  name: {{ include "kconsumer.fullname" . }}
  labels:
    {{- include "kconsumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kconsumer.selectorLabels" . | nindent 4 }}
