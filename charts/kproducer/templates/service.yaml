apiVersion: v1
kind: Service
metadata:
  name: {{ include "kproducer.fullname" . }}
  labels:
    {{- include "kproducer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kproducer.selectorLabels" . | nindent 4 }}
