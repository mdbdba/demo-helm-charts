apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "kproducer.fullname" . }}-test-connection'
  labels:
    {{- include "kproducer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kproducer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
